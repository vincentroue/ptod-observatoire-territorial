{
  "name": "pdtt-observable",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "observable preview",
    "build": "observable build",
    "deploy": "observable deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@observablehq/framework": "^1.13.3",
    "vitest": "^4.0.18"
  }
}
